apiVersion: v1
kind: Service
metadata:
  name: apache-deployment
  namespace: default
spec:
  selector:
    app: apache-deployment
  type: ClusterIP
  ports:
  - name: apache-deployment
    protocol: TCP
    port: 80
    targetPort: 80
