apiVersion: apps/v1
kind: Deployment
metadata:
  name:  hello-1
  namespace: default
  labels:
    app:  hello-1
spec:
  selector:
    matchLabels:
      app: hello-1
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app:  hello-1
    spec:
      containers:
      - name:  hello-1
        image: yogik001/test:v1
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort:  80
          name:  hello-1
      restartPolicy: Always